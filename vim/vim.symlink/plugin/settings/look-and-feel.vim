" Show the line numbers.
set number

" Show line currently being editted.
"set cursorline

" Highlight search results.
"set hlsearch
" Dynamically show search results as they are typed.
set incsearch

" Turn wrap off (just to experiment for now)
set nowrap

" Start scrolling after the cursor has made it to the 3rd line above the
" bottom.
set scrolloff=3

" Show a line ruler at 80 characters.
set colorcolumn=80
