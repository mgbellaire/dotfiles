if has("gui_macvim")
  " Use vim-pencil and vim-colors-pencil to better theme an environment for
  " editting markdown and text.
  set nonumber
  set relativenumber

  " No need for displaying what line your cursor is on for text files.
  set nocursorline

  " Turn off vertical line at 80 characters.
  set colorcolumn=0
endif
